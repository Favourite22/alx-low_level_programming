#include <stdio.h>

/**
 * main - program that prints the alphabet in lowercase, and then in uppercase,
 * You can only use putchar,
 * Return: 0
 */

int main(void)
{
int l= 'a';
int m= 'A';
while (l <= 'z')
}
putchar(l);
l +=1;
}
while (m <= 'Z')
{
putchar(m);
m +=1;
putchar ('\n');
return (0);
